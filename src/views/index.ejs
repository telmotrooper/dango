<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <h1><%= message %></h1>
    <textarea name="codebox" contenteditable="true"></textarea>
    <button type="submit">Send</button>

    <script>
      document.querySelector("[name=codebox]").addEventListener('keydown', (e) => {
      let keyCode = e.keyCode || e.which;

      if (keyCode === 9) {  // TAB = 9
        e.preventDefault();
        // document.execCommand('insertText', false, ' '.repeat(2));
        document.querySelector("[name=codebox]").value += "  "
      }
    });
    </script>
  </body>
</html>
